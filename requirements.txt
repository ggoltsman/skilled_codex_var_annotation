mcp>=1.9.0
pandas>=2.2.0
pyarrow>=15.0.0

# Optional system dependency for VCF normalization:
# bcftools. If unavailable, the project uses a small Python fallback that
# splits multiallelic ALT rows without reference-based left alignment.
